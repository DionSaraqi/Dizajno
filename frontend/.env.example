# Copy this file to `.env.local` and adjust as needed.
# Variables prefixed NEXT_PUBLIC_ are inlined into the browser bundle.

# Base URL of the Dizajno backend API.
NEXT_PUBLIC_API_URL=http://localhost:5000
